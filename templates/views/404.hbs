<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel="icon" href="/img/weather.png" />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <div class="main-content">
        {{>header}}
        <p>{{message}}</p>
        <a href={{goBackHref}}>Go back to {{goBackText}}.</a>
    </div>

    {{>footer}}
  </body>
</html>
